<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/home.css">
    <link rel="icon" href="/img/favicon.ico" type="image/x-icon" />
    <title>IndieWave</title>


</head>

<body>
    <%- include("../partials/header.ejs") %>
        <main>
            <div id="subtitle">
                <h2>
                    <%= title %>
                </h2>
            </div>
            <!-- Este primer for eventualmente va a volar cuando tengamos la base de datos -->
            <% for( let i=0; i < 5; i++ ) { %>
                <section id="category">
                    <!-- Hasta tener más productos (min 15 para que no se rompa por lo de html/body) mostramos solo 5 productos -->
                    <% for( let j=0; j < 5; j++ ) { %>
                        <article class="category-article">
                            <a href="/products/<%= categoryGames[j].id %> ">
                                <div>
                                    <img class="responsive-img" src="<%= categoryGames[j].logo%>"
                                        alt="Foto producto Indie Game">
                                </div>
                                <div class="detalle">
                                    <h3>
                                        <%= categoryGames[j].game_name%>
                                    </h3>
                                    <p>
                                        $<%= categoryGames[j].price%>
                                    </p>
                                </div>
                            </a>
                        </article>
                        <% } %>

                </section>
                <% } %>

        </main>
        <%- include("../partials/footer.ejs") %>
</body>

</html>