<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/forms.css">
    <title>IndieWave</title>

    <link rel="icon" href="/img/favicon.ico" type="image/x-icon" />
</head>

<body id="body-edit-create">

    <%- include("../partials/header.ejs") %>

        <main id="main-edit-create">
            <h2 id="title">Editar Producto <%= product.id %> </h2>
            <form id="create-form" action="" method="">

                <input type="text" name="product-name" id="product-name" aria-label="nombre del juego" value="<%= producto.game_name%>">

                <input type="text" name="dev" id="dev" aria-label="desarrollador" value="<%= producto.developer %>">

                <div id="gender">
                    <label for="categoria">Género:</label>
                    <select name="categoria">
                        <option value="arcade" 
                        <% if (producto.genre === "arcade" ){ %> 
                            selected 
                        <% } %> > Arcade </option>
                        <option value="accion"
                        <% if (producto.genre === "action" ){ %> 
                            selected 
                        <% } %> > Acción</option>
                        <option value="deportes"
                        <% if (producto.genre === "sports" ){ %> 
                            selected 
                        <% } %> > Deportes</option>
                        <option value="estrategia"
                        <% if (producto.genre === "strategy" ){ %> 
                            selected 
                        <% } %> > Estrategia</option>
                        <option value="aventura" 
                        <% if (producto.genre === "adventure" ){ %> 
                            selected 
                        <% } %> > Aventura</option>
                    </select>
                </div>

                <input type="email" name="email" id="email" aria-label="email" value="<%= producto.email %>">


                <label for="date">Fecha de lanzamiento</label>
                <input type="date" name="date" id="date" aria-label="fecha de lanzamiento" value="<%= producto.release_date %>">
                
                <span id="subtitle">Plataforma</span>
                <div id="checkbox-wrapper">

                    <label for="windows">Windows</label>
                    <input type="checkbox" name="windows" id="windows" 
                    <% if (producto.platform.includes("windows")) { %>
                        checked 
                    <% } %> > 

                    <label for="macos">macOS</label>
                    <input type="checkbox" name="macos" id="macos"
                    <% if (producto.platform.includes("macos")) { %>
                        checked 
                    <% } %> > 

                    <label for="linux">Linux</label>
                    <input type="checkbox" name="linux" id="linux"
                    <% if (producto.platform.includes("linux")) { %>
                        checked 
                    <% } %> > 

                </div>

                <input type="number" name="price" id="price" aria-label="price" value="<%= producto.price %>">

                <label for="logo" id="image" >Cargar Logo</label>
                <input type="file" name="logo" accept=".png, .jpeg, .jpg" id="logo" aria-label="logo" alt="logo">

                <label for="image" id="image" >Cargar Imágenes</label>
                <input type="file" name="image" accept=".png, .jpeg, .jpg" id="image" aria-label="imagen" alt="Imagen" multiple>

                <textarea rows="7" name="requeriment-min" id="requeriment-min" aria-label="requisitos minimos"><%= producto.min_requirements %></textarea>

                <textarea rows="7" name="requeriment" id="requeriment" aria-label="requeriment" ><%= producto.rec_requirements %></textarea>

                <textarea rows="9" name="descripcion" id="descripcion" maxlength="500" aria-label="descripcion"><%= producto.description %></textarea>
                
                <button type="reset" id="delete-btn">Borrar</button>

                <button type="submit" id="send-btn">Enviar</button>

                <div id="form-selection-wrapper">
                    <button id="create-btn"  aria-label="create"><a href="/products/create">Crear</a></button>
                    <button id="edit-btn"  aria-label="edit"><a href="/products/edit">Editar</a></button>
                </div>
                
            </form>
        </main>

        <%- include("../partials/footer.ejs") %>

</body>